spring:
  application:
    name: prostagma

  ai:
    ollama:
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: ${OLLAMA_MODEL:meltemi-en:latest}
          temperature: 0.1

  jackson:
    parser:
      allow-unescaped-control-chars: true
